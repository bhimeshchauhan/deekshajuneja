import * as React from "react";
import { Layout } from "../components/layout";

const SkillsPage = () => {
  return (
    <Layout bgColor={"#FFF"}>
      <div />
    </Layout>
  );
};

export default SkillsPage;
