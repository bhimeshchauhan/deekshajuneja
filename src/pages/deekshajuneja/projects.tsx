import * as React from "react";
import { Layout } from "../../components/layout";

const ProjectsPage = () => {
  return (
    <Layout>
      <div />
    </Layout>
  );
};

export default ProjectsPage;
